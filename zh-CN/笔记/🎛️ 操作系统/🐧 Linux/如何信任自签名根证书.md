---
tags:
  - 分类/操作系统
  - 操作系统/Linux
  - 运维
---
# 如何信任自签名根证书

```bash
# 安装包
apt install -y update-ca-certificates
# 将CA根证书瞎下载到指定目录并更新
wget https://xxx.xxx/ca.pem -O /usr/local/share/ca-certificates/ca.crt --no-check-certificate && update-ca-certificates
```
