---
tags:
  - 分类/运维
  - 软件/Docker
---
# 使用 scratch 空镜像

```dockerfile
FROM scratch
```

是的，你没有看错，没有标签没有仓库地址。只有一个单词。

与其说它是一个镜像不如说这是一个指令，用于告知Docker没有依赖基础镜像，将以下一条指令为基准开始构建。同时这个指令不会造成额外的层级（layer）。

## 适用场景

比如说你需要打包一个连系统都用不上的二进制程序？

例如有的时候C语言打包的程序已经把所有运行时和库文件都打包在自己身上，这种情况下就完全用不上系统，使用空镜像就能够做到理论最小体积的镜像。